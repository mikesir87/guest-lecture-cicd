name: Test code
on: [ push ]
jobs:
  test-app:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4.2.1

      - name: Install modules
        run: yarn install

      - name: Run tests
        run: yarn test
